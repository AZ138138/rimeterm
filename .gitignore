# Build output
/target/

# Rust
**/*.rs.bk
Cargo.lock.bak

# Design docs (kept local per repo owner's request; not pushed to public repo)
/docs/

# IDE / editor scratch
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db

# rimeterm runtime artefacts that could sneak in from a local run
/rimeterm-*.log
*.pid

# Stray glab-tui theme/config dirs materialised into the CWD by the
# upstream `$HOME`-less XDG fallback (`.`). These were historically
# written into the repo root when launched from a checkout.
/.config/
**/.config/glab-tui/

# Icon source (multi-res .ico is generated from this and shipped in-tree).
/rimeterm-icon.png

# planning-with-files skill scratchpads
/task_plan.md
/findings.md
/progress.md

# CI drop location for prebuilt yazi/gitui/bottom (see release.yml +
# .github/scripts/fetch-essentials.mjs). Also created when running
# `node bootstrap-essentials.mjs`.
/crates/rimeterm/essentials-staging/
